# Core — required for speak.py, home_announcer.py, batch.py, server.py
piper-tts>=1.2.0

# server.py uses Python's built-in http.server and urllib — no extra install needed.
# list_voices.py uses urllib.request — also stdlib, no extra install needed.

# Optional: MQTT integration (wire home_announcer.py to a smart home broker)
# paho-mqtt>=1.6
